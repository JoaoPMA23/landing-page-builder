{
  "name": "landing-page-builder",
  "private": true,
  "scripts": {
    "docker:backend:test": "docker compose exec backend npm test",
    "docker:backend:logs": "docker compose logs backend"
  }
}
